package Statastics;           //The file is Present in Package Statistics,it Contains the interface called stats

	public interface Stats      //Interface which contains the methods required to calculate the statistics
	{
		void strike_rate(float runs,float balls);
		void Economy_rate(float runs,float overs);
	}

